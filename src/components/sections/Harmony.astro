---
// c-harmony - Year showcase section with large numbers
interface Props {
  yearStart?: string;
  yearEnd?: string;
  caption?: string;
}

const {
  yearStart = "20",
  yearEnd = "25",
  caption = "Harmony in the making"
} = Astro.props;
---

<section class="c-harmony -t-p">
  <div class="-w">
    <div class="year-display">
      <div class="number-grid">
        <!-- Video placeholder in background -->
        <div class="video-bg">
          <div class="video-wrap">
            <div class="video-placeholder"></div>
          </div>
        </div>

        <!-- Large numbers -->
        <span class="tl">{yearStart}</span>
        <span class="tr">17</span>
        <span class="bl">—{yearStart}</span>
        <span class="br">{yearEnd}</span>

        <!-- Caption -->
        <span class="harmony-caption -mm -up">{caption}</span>
      </div>
    </div>

    <div class="content">
      <p class="-m">
        Since 2017, we've been crafting digital experiences that blend creativity with technical excellence. 
        Each year brings new challenges, new technologies, and new opportunities to push the boundaries of what's possible.
      </p>
      <div class="stats">
        <div class="stat">
          <span class="value -h4">55+</span>
          <span class="label -mm">Awards</span>
        </div>
        <div class="stat">
          <span class="value -h4">100+</span>
          <span class="label -mm">Projects</span>
        </div>
        <div class="stat">
          <span class="value -h4">8</span>
          <span class="label -mm">Years</span>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .c-harmony {
    background-color: var(--c-berry);
    color: var(--c-black);
    padding: calc(var(--g-gap) * 16) 0;
    overflow: hidden;
  }

  .c-harmony .-w {
    display: grid;
    grid-template-columns: repeat(var(--g-columns), 1fr);
    gap: var(--g-gap);
    row-gap: calc(var(--g-gap) * 8);
  }

  /* ========================================
     YEAR DISPLAY
     ======================================== */
  .year-display {
    grid-column: 1 / -1;
  }

  @media (min-width: 1024px) {
    .year-display {
      grid-column: 1 / 10;
    }
  }

  .number-grid {
    position: relative;
    display: grid;
    grid-template-columns: 1fr 1fr;
    grid-template-rows: 1fr 1fr;
    aspect-ratio: 16 / 10;
    border-radius: 1rem;
    overflow: hidden;
  }

  .number-grid .video-bg {
    position: absolute;
    inset: 0;
    z-index: 1;
  }

  .number-grid .video-bg .video-wrap {
    width: 100%;
    height: 100%;
  }

  .number-grid .tl,
  .number-grid .tr,
  .number-grid .bl,
  .number-grid .br {
    position: relative;
    z-index: 2;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: clamp(4rem, 15vw, 12rem);
    font-weight: 400;
    letter-spacing: -0.05em;
    line-height: 0.8;
    color: var(--c-white);
    mix-blend-mode: difference;
  }

  .number-grid .tl {
    justify-content: flex-start;
    align-items: flex-start;
    padding: calc(var(--g-gap) * 4);
  }

  .number-grid .tr {
    justify-content: flex-end;
    align-items: flex-start;
    padding: calc(var(--g-gap) * 4);
    color: rgba(var(--c-white-rgb), 0.3);
  }

  .number-grid .bl {
    justify-content: flex-start;
    align-items: flex-end;
    padding: calc(var(--g-gap) * 4);
    color: rgba(var(--c-white-rgb), 0.3);
  }

  .number-grid .br {
    justify-content: flex-end;
    align-items: flex-end;
    padding: calc(var(--g-gap) * 4);
  }

  .number-grid .harmony-caption {
    position: absolute;
    bottom: calc(var(--g-gap) * 4);
    left: 50%;
    transform: translateX(-50%);
    z-index: 3;
    color: var(--c-white);
    white-space: nowrap;
  }

  /* ========================================
     CONTENT
     ======================================== */
  .content {
    grid-column: 1 / -1;
    display: flex;
    flex-direction: column;
    gap: calc(var(--g-gap) * 8);
  }

  @media (min-width: 1024px) {
    .content {
      grid-column: 10 / 14;
      justify-content: center;
    }
  }

  .content p {
    color: var(--c-grey-dark);
    margin: 0;
    max-width: 35ch;
  }

  /* Stats */
  .stats {
    display: flex;
    gap: calc(var(--g-gap) * 4);
    flex-wrap: wrap;
  }

  .stat {
    display: flex;
    flex-direction: column;
    gap: calc(var(--g-gap));
  }

  .stat .value {
    color: var(--c-black);
  }

  .stat .label {
    color: var(--c-grey-dark);
  }

  /* Video placeholder */
  .c-harmony .video-placeholder {
    width: 100%;
    height: 100%;
    background: linear-gradient(
      180deg,
      var(--c-black) 0%,
      var(--c-grey-dark) 50%,
      var(--c-black) 100%
    );
    background-size: 100% 200%;
    animation: harmonyGradient 12s ease infinite;
  }

  @keyframes harmonyGradient {
    0%, 100% { background-position: 50% 0%; }
    50% { background-position: 50% 100%; }
  }
</style>
