---
// PreFooter.astro - Mirrored "AisacPixel" text section
---

<section class="pre-footer">
    <div class="mirror-container">
        <div class="mirror-text">Aisac&nbsp;Pixel</div>
        <div class="overlay-text">フィドル</div>
    </div>
</section>

<style>
    .pre-footer {
        background-color: var(--c-black);
        padding: 8rem 0 2rem 0; /* Reduced bottom padding to bring closer to footer */
        overflow: hidden;
        display: flex;
        justify-content: center;
        align-items: center;
    }

    .mirror-container {
        position: relative;
        display: flex;
        justify-content: center;
        align-items: center;
    }

    .mirror-text {
        font-size: 15vw;
        font-weight: 400;
        color: #1a1a1a; /* Dark grey, slightly lighter than BG */
        /* text-transform: uppercase; Removed to show AisacPixel case */
        letter-spacing: -0.02em;
        transform: scaleX(-1); /* Mirror horizontally */
        user-select: none;
        white-space: nowrap;
    }

    .overlay-text {
        position: absolute;
        top: 58%; /* Slightly lower than center (was 50%) */
        left: 50%;
        transform: translate(-50%, -50%);
        color: #ff0000; /* White as requested */
        font-size: 1.5rem; /* Small */
        letter-spacing: 0.1em;
        z-index: 10;
        pointer-events: none;
    }
</style>
